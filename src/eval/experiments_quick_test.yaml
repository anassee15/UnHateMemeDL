# Quick test config — exercises the whole pipeline on the smallest model.
# Usage: python run_experiment_grid.py --config experiments_quick_test.yaml --limit 3 --hateful_only

n_reps: 1

experiments:
  smoke:
    description: "Full pipeline smoke test (detection -> prompt -> diffusion -> metrics)"
    stages: [detection, prompt, diffusion, metrics]
    models:
      - Qwen/Qwen2.5-VL-7B-Instruct
